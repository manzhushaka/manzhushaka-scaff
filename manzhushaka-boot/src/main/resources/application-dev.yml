spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://127.0.0.1:3306/manzhushaka?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:1a2s3d4f}
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: ${REDIS_HOST:127.0.0.1}
      port: ${REDIS_PORT:6379}

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

storage:
  bos:
    endpoint: ${BOS_ENDPOINT:}
    bucket: ${BOS_BUCKET:}
    access-key-id: ${BOS_ACCESS_KEY_ID:}
    secret-access-key: ${BOS_SECRET_ACCESS_KEY:}
    base-path: ${BOS_BASE_PATH:import-export}
    download-expire-seconds: ${BOS_DOWNLOAD_EXPIRE_SECONDS:900}

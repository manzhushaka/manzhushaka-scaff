<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.manzhushaka.biz.pii.infrastructure.persistence.mapper.PiiPayQrcodeMapper">

    <resultMap id="BaseResultMap" type="com.manzhushaka.biz.pii.infrastructure.persistence.entity.PiiPayQrcode">
        <id property="id" column="id"/>
        <result property="merchantId" column="merchant_id"/>
        <result property="qrcodeCode" column="qrcode_code"/>
        <result property="qrcodeUrl" column="qrcode_url"/>
        <result property="qrcodeImageUrl" column="qrcode_image_url"/>
        <result property="name" column="name"/>
        <result property="status" column="status"/>
        <result property="expireTime" column="expire_time"/>
        <result property="remark" column="remark"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="createBy" column="create_by"/>
        <result property="updateBy" column="update_by"/>
        <result property="delFlag" column="del_flag"/>
    </resultMap>

    <sql id="BaseColumnList">id, merchant_id, qrcode_code, qrcode_url, qrcode_image_url, name, status, expire_time, remark, create_time, update_time, create_by, update_by, del_flag</sql>

    <insert id="insert" parameterType="com.manzhushaka.biz.pii.infrastructure.persistence.entity.PiiPayQrcode" useGeneratedKeys="true" keyProperty="id">
        insert into pii_pay_qrcode (merchant_id, qrcode_code, qrcode_url, qrcode_image_url, name, status, expire_time, remark, create_time, update_time, create_by, update_by, del_flag)
        values (#{merchantId}, #{qrcodeCode}, #{qrcodeUrl}, #{qrcodeImageUrl}, #{name}, #{status}, #{expireTime}, #{remark}, #{createTime}, #{updateTime}, #{createBy}, #{updateBy}, #{delFlag})
    </insert>

    <update id="updateById">
        update pii_pay_qrcode
        <set>
            <if test="merchantId != null">merchant_id = #{merchantId},</if>
            <if test="qrcodeCode != null">qrcode_code = #{qrcodeCode},</if>
            <if test="qrcodeUrl != null">qrcode_url = #{qrcodeUrl},</if>
            <if test="qrcodeImageUrl != null">qrcode_image_url = #{qrcodeImageUrl},</if>
            <if test="name != null">name = #{name},</if>
            <if test="status != null">status = #{status},</if>
            <if test="expireTime != null">expire_time = #{expireTime},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="delFlag != null">del_flag = #{delFlag},</if>
        </set>
        where id = #{id} and del_flag = 0
    </update>

    <update id="deleteById">
        update pii_pay_qrcode set del_flag = 2 where id = #{id} and del_flag = 0
    </update>

    <update id="updateStatus">
        update pii_pay_qrcode set status = #{status} where id = #{id} and del_flag = 0
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        select <include refid="BaseColumnList"/> from pii_pay_qrcode where id = #{id} and del_flag = 0
    </select>

    <select id="selectByCode" resultMap="BaseResultMap">
        select <include refid="BaseColumnList"/> from pii_pay_qrcode where qrcode_code = #{qrcodeCode} and del_flag = 0
    </select>

    <select id="selectByMerchantId" resultMap="BaseResultMap">
        select <include refid="BaseColumnList"/> from pii_pay_qrcode where merchant_id = #{merchantId} and del_flag = 0 order by id desc
    </select>

    <select id="selectExpiredBefore" resultMap="BaseResultMap">
        select <include refid="BaseColumnList"/> from pii_pay_qrcode where expire_time &lt; #{time} and del_flag = 0 limit #{limit}
    </select>

</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.manzhushaka.biz.pii.infrastructure.persistence.mapper.PiiTaxItemMapper">

    <resultMap id="BaseResultMap" type="com.manzhushaka.biz.pii.infrastructure.persistence.entity.PiiTaxItem">
        <id property="id" column="id"/>
        <result property="taxItemCode" column="tax_item_code"/>
        <result property="name" column="name"/>
        <result property="brevityCode" column="brevity_code"/>
        <result property="category" column="category"/>
        <result property="taxRate" column="tax_rate"/>
        <result property="vatSpecial" column="vat_special"/>
        <result property="freeTaxType" column="free_tax_type"/>
        <result property="preferPolicyFlag" column="prefer_policy_flag"/>
        <result property="sort" column="sort"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="createBy" column="create_by"/>
        <result property="updateBy" column="update_by"/>
        <result property="delFlag" column="del_flag"/>
    </resultMap>

    <sql id="BaseColumnList">id, tax_item_code, name, brevity_code, category, tax_rate, vat_special, free_tax_type, prefer_policy_flag, sort, status, remark, create_time, update_time, create_by, update_by, del_flag</sql>

    <insert id="insert" parameterType="com.manzhushaka.biz.pii.infrastructure.persistence.entity.PiiTaxItem" useGeneratedKeys="true" keyProperty="id">
        insert into pii_tax_item (tax_item_code, name, brevity_code, category, tax_rate, vat_special, free_tax_type, prefer_policy_flag, sort, status, remark, create_time, update_time, create_by, update_by, del_flag)
        values (#{taxItemCode}, #{name}, #{brevityCode}, #{category}, #{taxRate}, #{vatSpecial}, #{freeTaxType}, #{preferPolicyFlag}, #{sort}, #{status}, #{remark}, #{createTime}, #{updateTime}, #{createBy}, #{updateBy}, #{delFlag})
    </insert>

    <update id="updateById">
        update pii_tax_item
        <set>
            <if test="taxItemCode != null">tax_item_code = #{taxItemCode},</if>
            <if test="name != null">name = #{name},</if>
            <if test="brevityCode != null">brevity_code = #{brevityCode},</if>
            <if test="category != null">category = #{category},</if>
            <if test="taxRate != null">tax_rate = #{taxRate},</if>
            <if test="vatSpecial != null">vat_special = #{vatSpecial},</if>
            <if test="freeTaxType != null">free_tax_type = #{freeTaxType},</if>
            <if test="preferPolicyFlag != null">prefer_policy_flag = #{preferPolicyFlag},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="delFlag != null">del_flag = #{delFlag},</if>
        </set>
        where id = #{id} and del_flag = 0
    </update>

    <update id="deleteById">
        update pii_tax_item set del_flag = 2 where id = #{id} and del_flag = 0
    </update>

    <update id="updateStatus">
        update pii_tax_item set status = #{status} where id = #{id} and del_flag = 0
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        select <include refid="BaseColumnList"/> from pii_tax_item where id = #{id} and del_flag = 0
    </select>

    <select id="selectByTaxItemCode" resultMap="BaseResultMap">
        select <include refid="BaseColumnList"/> from pii_tax_item where tax_item_code = #{taxItemCode} and del_flag = 0
    </select>

    <select id="selectEnabled" resultMap="BaseResultMap">
        select <include refid="BaseColumnList"/> from pii_tax_item where status = 1 and del_flag = 0 order by sort asc, id asc
    </select>

    <select id="selectByNameLike" resultMap="BaseResultMap">
        select <include refid="BaseColumnList"/> from pii_tax_item where name like concat('%', #{name}, '%') and del_flag = 0 order by sort asc, id desc
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        select <include refid="BaseColumnList"/> from pii_tax_item
        where del_flag = 0
        <if test="taxItemCode != null and taxItemCode != ''">
            and tax_item_code like concat('%', #{taxItemCode}, '%')
        </if>
        <if test="name != null and name != ''">
            and name like concat('%', #{name}, '%')
        </if>
        <if test="status != null">
            and status = #{status}
        </if>
        order by sort asc, id desc
    </select>

</mapper>
